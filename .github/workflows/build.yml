name: Manual Windows Build

on:
  workflow_dispatch: # 允许在 GitHub Actions 页面手动点击运行

jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        config: [Release]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # 已升级到 v4

    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=${{ matrix.config }}

    - name: Build with MSVC
      run: cmake --build build --config ${{ matrix.config }}

    - name: Upload Artifact
      uses: actions/upload-artifact@v4 # 已升级到 v4，解决弃用报错问题
      with:
        name: Launchpad-RGB-Ambient-Windows-${{ matrix.config }}
        # 使用通配符匹配所有 Release 文件夹下的 exe，确保路径兼容性
        path: |
          **/bin/Release/*.exe
